%!
newpath
100 100 moveto % aller en x=100, y=100
200 150 lineto % relier ce point à x=200, y=150
100 200 lineto % relier à ce nouveau point
stroke         % trace la ligne

newpath
150 50 moveto
150 0 rlineto  % mouvement relatif
0 50 rlineto   % idem
-100 0 rlineto % idem
closepath      % retour au 1er point
.5 setgray
fill          % remplissage

/Times-Roman findfont % trouve une police et l'empile
20 scalefont        % la passe en taille 20 point
setfont             % la choisit pour le texte à venir
newpath      % du texte, c'est aussi un chemin
10 10 moveto % dont il faut placer l'origine,
(Hello) show % choisir le contenu, et l'afficher

showpage % fin du programme